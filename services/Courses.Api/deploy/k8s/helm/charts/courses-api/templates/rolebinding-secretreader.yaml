apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "courses-api.fullname" . }}-dapr-secret-reader
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "courses-api.labels" . | nindent 4 }}
subjects:
- kind: ServiceAccount
  name: {{ include "courses-api.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
roleRef:
  kind: Role
  name: {{ include "courses-api.fullname" . }}-secret-reader
  apiGroup: rbac.authorization.k8s.io
  