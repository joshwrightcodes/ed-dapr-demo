apiVersion: v1
kind: Service
metadata:
  name: {{ include "courses-api.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "courses-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "courses-api.selectorLabels" . | nindent 4 }}
