apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "courses-api.fullname" . }}-test-connection"
  labels:
    {{- include "courses-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "courses-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
